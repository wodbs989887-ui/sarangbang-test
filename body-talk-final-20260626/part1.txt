: 700; line-height: 1.45; word-break: keep-all; } .rules-nav { display: grid; grid-template-columns: 1fr auto 1fr; align-items: center; gap: 16px; padding-top: 14px; } .rules-nav button:first-child { justify-self: start; } .rules-nav button:last-child { justify-self: end; } .rules-page { font-size: clamp(16px, 1.6vw, 22px); font-weight: 800; font-variant-numeric: tabular-nums; } .game-screen { flex-direction: column; } .game-top { display: flex; justify-content: space-between; align-items: flex-start; font-size: clamp(28px, 4vw, 58px); font-weight: 800; font-variant-numeric: tabular-nums; } .game-word { flex: 1; display: flex; flex-direction: column; align-items: center; justify-content: center; text-align: center; min-height: 0; } .word-translation { margin: clamp(12px, 2vh, 24px) 0 0; font-size: clamp(24px, 3vw, 46px); line-height: 1.15; font-weight: 700; letter-spacing: -.015em; color: #555; word-break: keep-all; } .game-actions { display: grid; grid-template-columns: 1fr 1fr; gap: clamp(18px, 3vw, 44px); width: min(100%, 900px); margin: 0 auto; } .game-actions button { min-height: clamp(78px, 13vh, 120px); font-size: clamp(28px, 4vw, 52px); } .result-lines { margin: 0 0 clamp(28px, 5vh, 58px); font-size: clamp(34px, 5vw, 72px); font-weight: 800; line-height: 1.42; font-variant-numeric: tabular-nums; } .result-note { min-height: 1.5em; margin: -20px 0 24px; } .save-actions { display: flex; flex-wrap: wrap; justify-content: center; gap: 14px; } .summary-screen { flex-direction: column; overflow-y: auto; } .summary-title { margin: 0 0 clamp(18px, 3vh, 36px); text-align: center; font-size: clamp(38px, 5vw, 70px); } .score-grid { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: clamp(18px, 3vw, 44px); width: min(100%, 1300px); margin: 0 auto; } .score-card { border: var(--line) solid #000; padding: clamp(16px, 2.5vw, 34px); } .score-card h2 { margin: 0 0 18px; font-size: clamp(28px, 3.2vw, 46px); } .score-row { display: grid; grid-template-columns: minmax(130px, 1.15fr) repeat(3, minmax(72px, .75fr)); gap: 8px; padding: 12px 0; border-bottom: 1px solid #aaa; font-size: clamp(15px, 1.65vw, 24px); line-height: 1.3; font-variant-numeric: tabular-nums; } .score-row > span:not(:first-child) { text-align: right; } .score-total { margin: 18px 0 0; font-size: clamp(18px, 2vw, 28px); font-weight: 800; line-height: 1.65; } .summary-actions { display: flex; justify-content: center; gap: 14px; margin-top: clamp(20px, 3vh, 38px); } .editor-screen { flex-direction: column; gap: 20px; overflow-y: auto; } .editor-header { text-align: center; } .editor-title { margin: 0 0 8px; font-size: clamp(36px, 4.5vw, 64px); } .editor-help { margin: 0; font-size: clamp(16px, 1.7vw, 23px); line-height: 1.5; } .editor-example, .editor-field { display: grid; gap: 8px; font-size: clamp(16px, 1.5vw, 21px); font-weight: 700; } .editor-example { width: min(100%, 1100px); margin: 0 auto; } .editor-grid { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: clamp(16px, 2.5vw, 32px); width: min(100%, 1300px); margin: 0 auto; } .editor-card { border: var(--line) solid #000; padding: clamp(16px, 2vw, 28px); } .editor-card h2 { margin: 0 0 16px; font-size: clamp(26px, 2.8vw, 40px); } .editor-fields { display: grid; gap: 14px; } .editor-field textarea, .editor-example textarea { width: 100%; min-height: 76px; resize: vertical; padding: 12px; border: 2px solid #000; border-radius: 0; background: #fff; color: #000; font: inherit; font-weight: 500; line-height: 1.45; } .editor-field textarea:focus, .editor-example textarea:focus { outline: 4px solid #000; outline-offset: 2px; } .editor-message { min-height: 1.5em; margin: 0; text-align: center; font-size: clamp(16px, 1.6vw, 22px); font-weight: 700; } .editor-actions { position: sticky; bottom: calc(-1 * var(--edge)); display: flex; flex-wrap: wrap; justify-content: center; gap: 12px; padding: 18px 0 calc(8px + env(safe-area-inset-bottom)); background: #fff; } @media (max-width: 760px), (max-height: 560px) { :root { --edge: 18px; --line: 2px; } button { min-height: 54px; padding: 10px 18px; } .round-list { gap: 10px; grid-template-columns: repeat(3, 1fr); } .round-button { min-width: 0; min-height: 115px; padding-inline: 8px; } .corner.right { bottom: calc(var(--edge) + 72px + env(safe-area-inset-bottom)); } .time-control { gap: 7px; } .time-control button { min-width: 76px; padding-inline: 10px; } .score-row { grid-template-columns: minmax(100px, 1fr) repeat(3, minmax(58px, .6fr)); } .rules-title { font-size: clamp(30px, 5.5vw, 48px); } .rules-lead { margin-block: 10px; font-size: clamp(22px, 3.6vw, 34px); } .rules-flow { gap: 7px; } .rules-flow li { grid-template-columns: 36px 1fr; gap: 9px; font-size: clamp(18px, 2.65vw, 27px); } .rule-number { width: 32px; height: 32px; font-size: 18px; } .rules-score { margin-top: 10px; font-size: clamp(22px, 3.3vw, 34px); } .rules-callout { margin-top: 8px; font-size: clamp(21px, 3vw, 31px); } .rules-ban { margin-top: 6px; } .r