<!doctype html> <html lang='ko'> <head> <meta charset='utf-8'> <meta name='viewport' content='width=device-width, initial-scale=1, viewport-fit=cover'> <meta name='theme-color' content='#ffffff'> <title>몸으로 말해요</title> <style> :root { color-scheme: light; font-family: Arial, 'Apple SD Gothic Neo', 'Noto Sans KR', sans-serif; --edge: clamp(20px, 4vw, 64px); --line: 3px; } * { box-sizing: border-box; } html, body { width: 100%; height: 100%; margin: 0; overflow: hidden; background: #fff; color: #000; } body { min-height: 100dvh; -webkit-tap-highlight-color: transparent; touch-action: manipulation; } button { min-height: 64px; padding: 14px 28px; border: var(--line) solid #000; border-radius: 0; background: #fff; color: #000; font: inherit; font-size: clamp(18px, 2.1vw, 30px); font-weight: 700; cursor: pointer; } button:hover { background: #f2f2f2; } button:active { background: #000; color: #fff; } button:focus-visible { outline: 4px solid #000; outline-offset: 4px; } button:disabled { border-color: #aaa; color: #999; background: #f5f5f5; cursor: not-allowed; } #app { width: 100%; height: 100%; } .screen { position: relative; display: flex; width: 100%; height: 100%; min-height: 100dvh; padding: calc(var(--edge) + env(safe-area-inset-top)) calc(var(--edge) + env(safe-area-inset-right)) calc(var(--edge) + env(safe-area-inset-bottom)) calc(var(--edge) + env(safe-area-inset-left)); animation: appear 120ms ease-out; } @keyframes appear { from { opacity: 0; } to { opacity: 1; } } @media (prefers-reduced-motion: reduce) { .screen { animation: none; } } .center { display: flex; flex: 1; min-width: 0; flex-direction: column; align-items: center; justify-content: center; text-align: center; } .hero-title { margin: 0 0 clamp(30px, 6vh, 70px); font-size: clamp(56px, 10vw, 150px); line-height: 1.08; letter-spacing: -.055em; word-break: keep-all; } .category-title { margin: 0 0 clamp(20px, 4vh, 44px); font-size: clamp(64px, 12vw, 170px); line-height: 1; letter-spacing: -.04em; word-break: keep-all; } .word { max-width: 92%; margin: 0; letter-spacing: -.045em; word-break: keep-all; overflow-wrap: anywhere; } /* 제시어 길이에 따라 화면을 넘지 않으면서 최대한 크게 보이도록 조절합니다. */ .word-short { font-size: clamp(64px, 11vw, 170px); line-height: 1.08; } .word-medium { font-size: clamp(52px, 8vw, 122px); line-height: 1.1; } .word-long { font-size: clamp(40px, 6vw, 90px); line-height: 1.18; } .word-extra-long { font-size: clamp(32px, 4.8vw, 72px); line-height: 1.24; } .small-note { margin: 8px 0; font-size: clamp(18px, 2.2vw, 32px); line-height: 1.45; word-break: keep-all; } .muted { color: #555; } .start-button { min-width: 180px; } .corner { position: absolute; bottom: calc(var(--edge) + env(safe-area-inset-bottom)); z-index: 2; } .corner.left { left: calc(var(--edge) + env(safe-area-inset-left)); } .corner.right { right: calc(var(--edge) + env(safe-area-inset-right)); } .time-control { display: flex; align-items: center; gap: 12px; } .time-control button { min-width: 100px; padding-inline: 18px; } .time-value { min-width: 110px; font-size: clamp(22px, 2.5vw, 36px); font-weight: 800; text-align: center; font-variant-numeric: tabular-nums; } .round-list { display: grid; grid-template-columns: repeat(3, minmax(190px, 1fr)); gap: clamp(16px, 2.5vw, 36px); width: min(100%, 1000px); } .round-button { min-height: clamp(145px, 24vh, 230px); font-size: clamp(27px, 3.2vw, 46px); } .round-button small { display: block; margin-top: 12px; font-size: .6em; font-weight: 500; } .intro-details { margin-bottom: clamp(28px, 5vh, 58px); } .rules-screen { flex-direction: column; } .rules-title { margin: 0; text-align: center; font-size: clamp(38px, 5vw, 70px); line-height: 1.12; letter-spacing: -.045em; word-break: keep-all; } .rules-content { display: flex; flex: 1; min-height: 0; flex-direction: column; align-items: center; justify-content: center; width: min(100%, 1120px); margin: 0 auto; text-align: center; } .rules-lead { margin: clamp(18px, 3vh, 32px) 0; font-size: clamp(28px, 3.4vw, 48px); font-weight: 800; line-height: 1.3; word-break: keep-all; } .rules-flow { display: grid; gap: clamp(10px, 1.6vh, 18px); width: min(100%, 980px); margin: 0; padding: 0; list-style: none; text-align: left; } .rules-flow li { display: grid; grid-template-columns: 48px 1fr; align-items: center; gap: 14px; font-size: clamp(23px, 2.55vw, 36px); font-weight: 700; line-height: 1.28; word-break: keep-all; } .rule-number { display: inline-flex; align-items: center; justify-content: center; width: 42px; height: 42px; border: 2px solid #000; border-radius: 50%; font-size: 23px; font-weight: 800; } .rules-score { margin: clamp(18px, 3vh, 30px) 0 0; font-size: clamp(27px, 3.2vw, 46px); font-weight: 900; line-height: 1.25; word-break: keep-all; } .rules-callout { margin: clamp(14px, 2.4vh, 26px) 0 0; font-size: clamp(25px, 3vw, 42px); font-weight: 800; line-height: 1.25; word-break: keep-all; } .rules-ban { margin: clamp(10px, 1.8vh, 18px) 0 0; font-size: clamp(15px, 1.55vw, 22px); font-weight